using System;

public class createNewGoals: menuOption
{
   public simpleGoals()
   {
      Console.WriteLine.log(createNewGoals);
      
   }
   static int Choice()
   {
      string Choice = @"Select a choice from a menuOption
      1.Creat NewGoals
      2.List Goals
      3.Save  Goals
      4.Load Goals
      5.Record Events
      6.Quit
      What would you like to do?";
   }
    string filename = "myFile.txt";
   String [] line = System.IO.File.ReadAllLines(store.txt);
   foreach(string line in lines)
   {
    string [] parts = line.split(",");
   }
   class string fileName = "myFile.txt";
   using streamWriter outputFile = new streamWriter(fileName)
   {
    outputFile.WriteLine("");
    string word = "I want to wake every morning by 5:30am";
    outputFile.WriteLine($"Your simple is {word}");
   }

   string eternalGoals();
   {
    Console.WriteLine.log(createNewGoals);
   }
    string filename = "myFile.txt";
   string [] line = System.IO.File.ReadAllLines(store.txt);
   foreach(string line in lines)
   {
    string [] parts = line.split(",");
   }
   class  fileName = "myFile.txt";
   using streamWriter outputFile = new streamWriter(fileName)
   {
    outputFile.WriteLine("");
    string word = "I want to be reading scripture everyday";
    outputFile.WriteLine($"Your simple is {word}");
   }
   // for every complete goal you earn point
   const string point = 0;
   {
    if(complete == true);
    points = 50;
    Console. WriteLine.log("Congrantulation!");
   }
   // for every complete goal you earn bouns point 
   const string bounsPoint = 100;
   {
      foreach(createNewGoals == complete);
      bounsPoint = 100;
      Console. WriteLine.log("Good effort! keep it up");

   }

}