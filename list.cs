using System,

public class listGoals: menuOption
{
   public listGoals()
   {
     Console.WriteLine.log(listGoals);
   }
   private class listGoals()
   {
     _listGoals = listGoals;
   }
   public virtual void listGoals
   {
       Console.WriteLine.log(listGoals);
   }
    string filename = "myFile.txt";
   string [] line = System.IO.File.ReadAllLines(store.txt);
   foreach(string line in lines)
   {
    string [] parts = line.split(",");
   }
   class  fileName = "myFile.txt";
   using streamWriter outputFile = new streamWriter(fileName)
   {
    outputFile.WriteLine("");
    string word = "I want to be reading scripture everyday";
    outputFile.WriteLine($"Your simple goal is {word}");
   }
   
   public override void createNewGoals
   {
     Console.WriteLine("I love playing chess");
   }
}