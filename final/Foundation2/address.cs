using System;

class address
{
    private string _street;
    private string _city;
    private string _state;
    private string _country;
    private bool _liveUSA;

    public address(street, city, state, country)
    (
        _street = street;
        _city = city;
        _state = state;
        _country = country;
    )

    if(country = USA)
    {
        return true;
    }
    else
    {
       return false;  
    }

    public FullAddress()
    {
        string fullAddress;

        FullAddress = $"{stree} {city} {state} {country}"
    }
}